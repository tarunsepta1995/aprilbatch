name : build and upload artifacts on docker hub

on:
    push:
        branches: [ main, master ]
    pull_request:
        branches: [ main, master ]
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
        
        steps:
        - name: Checkout code
          uses: actions/checkout@v4
          
        - name: Build Docker image
          run: docker build -t java-sample-app ./java-docker-example
          
        - name: Log in to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_PASSWORD }}
            
        - name: Tag the Docker image
          run: docker tag java-sample-app java-sample-app:1.0.${{ github.run_number }}
        
        - name: Push Docker image to Docker Hub
          run: docker push tarunsepta/java-sample-app:1.0.${{ github.run_number }}
        